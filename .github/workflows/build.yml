name: Build

on:
    push:
        tags-ignore:
            - '*'
        branches:
            - '**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build
        uses: actions/setup-node@v3
        with:
          node-version: 18
    
